import { Layout } from 'lib/components'
import {
  Image,
  Display,
  Text,
  Code,
  Link,
  Dot,
  Divider,
  Note,
  Spacer,
  Tag,
  Grid,
} from '@geist-ui/core'

export const meta = {
  title: '单词',
  date: '',
}

export const Words = () => {
  const words = [
    'synonym',
    'erect',
    'nuisance',
    'readily',
    'controversial',
  ];
  return words.map(_ => <Word>{_}</Word>);
};

export const Word = ({ children }) => (
  <>
    <Tag type="success" mb={0.8}>
      <Link href={`https://www.vocabulary.com/dictionary/${children}`} target="_blank">{children}</Link>
    </Tag>
    <Spacer inline />
  </>
);

<Words />

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
