import { Layout } from 'lib/components'
import {
  Image,
  Display,
} from '@geist-ui/core'

export const meta = {
  title: '代码片段',
  date: '',
}


<Display
  shadow
  caption="有时候需要获取到最新的state"
>
  <Image shadow src="/imgs/code/useStateRef.png" />
</Display>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
